void main() {
  const myAge = 44;
  const isTeenager = 13 <= 19;
  const maryAge = 30;
  const bothTeenagers = myAge == maryAge && isTeenager;
  print('A result is $bothTeenagers both you and mary not a teenagers');
  const reader = 'Riyad';
  const ray = 'Ray Wenderlich';
  const rayIsReader = ray == reader;
  print('\n');
  print(rayIsReader);
}
